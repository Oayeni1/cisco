# This is the list of interface speeds in Mbps
interface_speeds = [100, 300, 500, 234, 342]

# Now calculating the total bandwidth
total_bandwidth = sum(interface_speeds)

# Display the result
print(f"Interface Speeds (Mbps): {interface_speeds}")
print(f"Total Bandwidth Usage: {total_bandwidth} Mbps")
